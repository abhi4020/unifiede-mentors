/*
  Copy this file to `emailjs-config.js` (do NOT commit your actual keys to source control)
  and replace the placeholder values with your EmailJS credentials.

  Sign up at https://www.emailjs.com/ and create a service and a template.

  Example:
    window.emailjsConfig = {
      serviceId: 'service_xxx',
      templateId: 'template_yyy',
      publicKey: 'user_zzzz',
      // optional: override recipient if desired
      toEmail: 'abhi4020@yopmail.com'
    };

  After creating `emailjs-config.js` in the same folder, the site will auto-load it
  and enable direct sending from the contact form.
*/

/* Paste your config below (example values) */
// window.emailjsConfig = {
//   serviceId: 'your_service_id',
//   templateId: 'your_template_id',
//   publicKey: 'your_public_key',
//   toEmail: 'abhi4020@yopmail.com'
// };
